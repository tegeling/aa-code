// select User Id via SOQL
// select Id, Email, FirstName, LastName, LastLoginDate from User where IsActive = false and FirstName != null order by LastLoginDate
UserManagement.obfuscateUser('0051p00000D9dHMAAZ');
